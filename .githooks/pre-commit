!/bin/bash
set -e  # exit on any error

./tooling/new_line.sh
./tooling/update_header_license.sh

# if both scripts succeed, we will allow the commit
exit 0

# needed on new systems
#chmod +x .git/hooks/pre-commit
#git config core.hooksPath .githooks